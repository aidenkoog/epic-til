# 데이터베이스

정리될 내용들은 아래와 같습니다.

- 개념 / 용어 정의, 사용 이유
- Interview Question, Answer

## 정규화

- 1NF (제 1정규형)
  - 테이블(릴레이션)에 각 도메인은 원자성(Atomicity)를 가짐, 단일값
- 2NF (제 2정규형)
  - 1NF 충족된 상태에서 진행
  - 완전 함수 종속을 만족하도록 테이블 분해하는 것
  - 기본키의 부분집합이 결정자가 되어선 안됨, 즉 여러개의 조합인 기본키의 일부로 다른 컬럼의 값을 결정하면 안됨